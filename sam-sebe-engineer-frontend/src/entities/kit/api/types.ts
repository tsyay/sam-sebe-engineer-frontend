export type KitDto = {
        kitId: number,
        title: string,
        description: string,
        shortDescription: string,
        images: string,
        componentIds: number[],
        instructionIds: number[] 
}